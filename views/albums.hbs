<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Albums</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <h2 id="albums">Albums for {{artistName}}</h2>

    <ul>
        {{#each albums}}
        <li>
            <h3>{{this.name}}</h3>
            {{#if this.images.[0]}}
            <img src="{{this.images.[0].url}}" alt="{{this.name}} Album Cover" width="200" height="200">
            {{else}}
            <p>Sorry, no album cover available</p>
            {{/if}}
            <a href="/albumTracks/{{this.id}}">View Tracks</a>

        </li>
        {{/each}}

    </ul>
    
</body>
</html>
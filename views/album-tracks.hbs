<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tracks</title>
</head>
<body>
    <h2>Tracks for: {{albumId}}</h2>

    <ul>
        {{#each tracks}}
        <li>
            {{track_number}}.{{name}}
            {{#if preview_url}}
            <audio controls>
                <source src="{{preview_url}}" type="audio/mpeg">
                Sorry, your browser does not support the audio element.
            </audio>
            {{/if}}
        </li>
        {{/each}}
    </ul>
    
</body>
</html>